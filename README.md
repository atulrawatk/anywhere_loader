# Anywhere Loader - A GetX-Based Flutter Loader

## Features
✅ Uses **Overlay**, no need for Stack.  
✅ Works with **async functions**.  
✅ Supports **custom fonts, colors, and widgets**.

## Installation
Add this to `pubspec.yaml`:
```yaml
dependencies:
  anywhere_loader: ^1.0.0


